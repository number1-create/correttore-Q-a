services:
  - type: web
    name: qa-corrector
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run Qa_corrector.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
